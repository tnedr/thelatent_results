# Activation digest validate 0810

Dispatch: `dispatch_20260628_0810_home_activation_digest_validate`

Model: `gpt2`, n=64, seed=0. Byte-parity: digest `run_on` vs live probe entry.

**DRIFT (9/10 pass)** (speedup vs 10 separate live probes: 4.64x)
