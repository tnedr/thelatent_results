# IRREPCAP 0749 — home p=401 GPU tail

Dispatch: `dispatch_20260627_0749_home_irrepcap_scale`

Corp owns p=97 + p=211. Home adds **p=401** on `IRREP_DEVICE=cuda`:
readout-attribution + multiseed, ops + * -, seeds 0,1,2, max_steps 6000.

Outputs under `pinv/p401/` (phase subdirs). Does not overwrite corp p=97/p=211 JSONs.
